| requirementId | sourceRequirement | implementation | automatedTest | command | status | artifact | residualRisk | commit |
|---|---|---|---|---|---|---|---|---|
| T6 | B06-01..B06-05: Structured Ren'Py text records | `RenpyTextRecord` fields plus `RpycTextExtractor.extractRecords`; legacy `extractTexts` projection retained | Focused B06 harness PASS; Phase 1 remediation `237e444` closes the stale workshop/guidance contracts; full scanner and nested discover PASS | Focused 4-test command PASS; `python -m unittest test_fast_scanner.py -v` = 71 tests, 0 failures; `python -m unittest discover -s . -p 'test_*.py' -v` = 179 tests, 0 failures, 1 explicit skip | PASS | JVM/stub extractor harness plus formally rebuilt `apk-work/slg-workshop-ui-signed.apk`; no device or real Ren'Py corpus claim | B06-01..B06-05 are verified by the listed tests. The explicit audit-fixture skip remains a missing-fixture boundary, not a fabricated pass. | `b4f4696`, `237e444` |
| T7 | B07-01..B07-05: Official Ren'Py marked strings and exact keys | Existing `RpycTextExtractor.extractRecords` consumes `_()`, `__()`, `___()` and `_p()` forms; preserves legal prefixes, triple-quote boundaries and exact `{#...}` keys; dynamic calls remain uncertain | Three focused JVM/Java harness tests PASS; full scanner and nested discover PASS | Focused 3-test command PASS (3 tests, 0 failures); `python -m py_compile test_fast_scanner.py` exit 0; `python -m unittest test_fast_scanner.py -v` = 71 tests, 0 failures; `python -m unittest discover -s . -p 'test_*.py' -v` = 179 tests, 0 failures, 1 explicit skip | PASS | Actual extractor behavior through repository JVM stubs; no real APK/device or real Ren'Py corpus claim | `Save{#slot}` and `Save{#menu}` are asserted as distinct exact keys. The dynamic-call case is explicitly reported uncertain. The explicit audit-fixture skip remains a missing-fixture boundary, not a fabricated pass. | `98d5fdd` |
| T8 | Context collision detection and structured corpus | `RenpyTranslationCorpus` preserves exact old text, every occurrence and contextual candidates; UI report exposes counts; cache identity and the independent compiler/RPYC path retain marker keys | B08-01..B08-06 focused harnesses PASS; quality/coverage/workshop/scanner and nested discover PASS | Focused B08: 6 tests/1.285s, OK; `py_compile`: exit 0; quality: 16 tests/10.274s, OK; coverage: 12 tests/3.190s, OK (1 explicit skip); workshop: 59 tests/8.096s, OK; scanner: 71 tests/108.787s, OK; nested discover: 180 tests/134.080s, OK (1 explicit skip) | PASS | JVM/stub corpus, real compiler/RPYC extractor harnesses and generated-JS cache harness; no real device or real third-party Ren'Py corpus claim | The audit-coverage test remains skipped because its APK/extracted-text fixture is absent. Conflicting translations are rejected before compile, identical translations generate a validated artifact, collision export is sanitized. B08-06 separately proves the cache path and the compiler/RPYC path; cache→compile end-to-end chaining is not verified. | `54d22d1`, `4c740cf`, `bc042f0`, `33d083c`, `4a1a0a2`, `eead002` |
| T9 | B09-01..B09-06: Ren'Py placeholder, markup, interpolation and printf double gates | `RenpyTextValidator.validate`; real `LocalLlmEngine.acceptTranslation` model-acceptance gate; `TranslationCompiler.compileTranslationArtifact` compile gate | Full executable matrix: 4 valid pairs reach compiler and produce non-empty RPYC; 18 invalid pairs are rejected by validator, model gate and compile gate with stable visible codes | Required focused command: 2 tests/7.252s, OK; matrix: 1 test/4.724s, OK; `py_compile`: exit 0; quality: 17 tests/13.574s, OK; scanner: 71 tests/107.644s, OK; workshop: 59 tests/8.128s, OK; coverage: 12 tests/3.167s, OK (1 explicit skip); nested discover: 181 tests/136.762s, OK (1 explicit skip) | PASS | JVM/stub execution of validator, LocalLlmEngine acceptance and compiler/RPYC writer | no real Android/Capacitor/device/third-party corpus claim; the only skip is the existing missing audit fixture | `1cea61d`, `15ee764` |
| T10 | B10-01..B10-06: Coverage report, classification, incremental cache and build gates | Existing `TranslationCoverageReport` report, generated coverage UI, exact-text/state reset and explicit audit-fixture branch satisfy the contract; no production change was required | Original four-test gate plus four new generated-JS/Java gap harnesses PASS; coverage, quality, workshop, scanner and nested discover PASS; audit corpus branch remains an explicit skip | Original focused: 4 tests/3.142s, OK; added gap focused: 4 tests/0.938s, OK; `py_compile`: exit 0; coverage: 16 tests/4.178s, OK (1 skip); quality: 17 tests/13.076s, OK; workshop: 59 tests/8.284s, OK; scanner: 71 tests/107.342s, OK; nested discover: 185 tests/136.873s, OK (1 skip) | PASS | JVM/Node/Python controlled fixtures and generated JS behavior; audit APK/extracted-text fixture is absent, so no real-corpus missing count is claimed; no device test | Missing/rejected counts block complete build; explicit incomplete patch remains visibly warned; sanitized report excludes translation values and secrets; incremental cache-hit composition is verified in generated JS fixtures, not a real Android/Capacitor runtime | `b0d0b22`, `6224e86`, `caf2467`, `941b872` |
| T11 | B11-01..B11-06: Chinese font coverage and CJK line-break diagnostics | `RenpyFontSupport.defaultRequiredCodePoints`, `codePointsOfTranslations`, `inspect`; `TranslationCompiler.compileTranslationsIntoApk` final font gate and `cloneChineseStyleRpyc`; existing production path passed without a production change | Original four B11 focused tests plus the new accepted-translation glyph gate PASS; `py_compile` PASS; scanner, workshop and nested discover PASS | Focused B11: 5 tests/9.078s, OK; `py_compile`: exit 0; scanner: 72 tests/121.871s, OK; workshop: 59 tests/8.647s, OK; nested discover: 186 tests/142.675s, OK (1 explicit audit-fixture skip) | PASS | Controlled APK ZIP/JVM Android-stub fixtures; no real Android/Capacitor runtime, device, or real Ren'Py corpus claim | Fixed baseline preflight and accepted-translation required sets are tested separately; a baseline-absent `界` is added to the final set, reported missing when the candidate does not cover it, and blocks compile; real corpus/font rendering remains outside this task | `798860c` |
| T12 | Unified compatibility preflight report and UI | `RenpyPreflight.inspect`, `RenpyCompatibilityReport`, generated `Ce` compatibility gate and `FastApkScanner.readRenpyTexts` bridge; no production change was required | B12-01..B12-05 focused Java/Node harnesses PASS; unsupported snapshot asserts `UNSUPPORTED` + `NONE` + `renpy_preflight_missing_source`, zero extraction/model/build calls, and sanitized export; scanner/workshop/discover regressions PASS | Focused B12: 5 tests/5.283s, OK; `py_compile`: exit 0; scanner: 73 tests/116.592s, OK; workshop: 59 tests/8.353s, OK; nested discover: 187 tests/138.607s, OK (1 explicit audit-fixture skip); evidence parser PASS; `git diff --check` PASS | PASS | Controlled JVM/Node/generated-JS fixtures verify SAFE/WARNING/EXTRACT_ONLY and unsupported blocking; no real Android/Capacitor runtime, device, or real Ren'Py corpus claim | Unsupported source selection fails closed before extraction, model, or build; sanitized report omits translation values/secrets; generated UI gate execution is verified offline and does not establish installed-device behavior | `1d6e927` |
| T13 | Python 2 / protocol 2 restricted writer | RpycPickleWriter.Dialect.PY2_PROTOCOL_2, RpycCompatibility capability matrix and TranslationCompiler language/path selection; no production Java change was required | C13-01..C13-06 executable writer, independent opcode reader, capability, adversarial and nullable always-on tests PASS; modern golden remains byte-stable; helper DEX rebuild and ownership check PASS | Focused C13: 5 tests/15.216s, OK; py_compile: exit 0; scanner: 74 tests/120.443s, OK; workshop: 59 tests/8.499s, OK; nested discover: 188 tests/149.757s, OK (1 explicit audit-fixture skip); python apk-work/native-fast-scan/build_fast_scanner.py: exit 0 via D8 response-file build; classes7.dex writer descriptors exactly once | PASS | Controlled JVM/RPYC fixtures, independent non-executing byte reader and generated helper DEX; no real Ren'Py corpus, Android runtime, device or Python 2 interpreter claim | Protocol-2 output is restricted to the verified writer shape and allowed opcode set; unknown/adversarial graphs remain extract-only; always-on uses pickle NONE and tl/None, selectable remains slgtranslated; no unsafe deserialization or GLOBAL execution | 31e679b |
| T14 | Split APK scan, re-signing, and session install | InstalledApkSet immutable base/splits, InstalledAppSource manifest-name selection, FastApkScanner per-APK ownership/font merge and PackageInstallerSupport pre-session metadata validation/one-session write path; C14 fixed two production gaps | C14-01..C14-06 executable Java harnesses and split contracts PASS; manifest split selection preserves names/copies; installer harness verifies base→declared split order, package/version/signature/split rejection before session, write-failure abandon and duplicate-name fail-closed; scanner/workshop/discover and helper DEX PASS | Focused C14: 8 tests/4.734s, OK; py_compile: exit 0; scanner: 76 tests/122.278s, OK; workshop: 59 tests/8.462s, OK; nested discover: 190 tests/149.812s, OK (1 explicit audit-fixture skip); python apk-work/native-fast-scan/build_fast_scanner.py: exit 0; classes7.dex has one descriptor each for InstalledApkSet, InstalledAppSource, FastApkScanner and PackageInstallerSupport | PASS | Controlled JVM/ZIP/PackageInstaller fixtures and generated helper DEX; real split APK installation, Android PackageInstaller behavior, device launch and rollback are NOT-RUN because no approved real split target/device session was exercised | Validation is fail-closed before createSession for package/version/signature/split mismatch; write failure abandons session; source-level split ownership and UI metadata are verified offline; fixture PASS must not be promoted to a real-device PASS | df89e60 |
| T15 | Advanced dialogue-ID translation mode and rollback boundary | `RenpyDialogueTranslation` rejects unsafe identifiers; `RpycTextExtractor` exposes only verified `Say`/`TranslateSay` IDs; `RpycPickleWriter.isDialogueIdWriterVerified` admits only `PY3_MODERN` AST 17; generated UI keeps the mode default-off and requires all four capability flags | C15-01..C15-06 executable Java/Node/Python gates PASS except the device-only save/rollback row, which remains explicit NOT-RUN; unsafe IDs, protocol 2, non-17 ASTs and duplicate-ID collisions fail closed before `TranslateSay`; mixed dialogue/menu/character/UI output is retained | Focused C15: 4 tests/7.068s, OK; `py_compile`: exit 0; scanner: 77 tests/181.534s, OK; workshop: 60 tests/12.389s, OK; nested discover: 192 tests/203.185s, OK (1 explicit audit-fixture skip); workshop APK rebuild exit 0 and asset-pinning test PASS; `python apk-work/native-fast-scan/build_fast_scanner.py`: exit 0; `classes7.dex` has one descriptor each for `RenpyDialogueTranslation`, `RpycPickleWriter`, `RpycTextExtractor` and `FastApkScanner` | PASS | Controlled JVM/Node fixtures and formally rebuilt local APK/helper DEX; new-game/old-save/rollback/jump/selectable/always-on real-game matrix and installed-device behavior are NOT-RUN; the advanced mode remains release-unapproved until C15-05 is exercised on an approved real target | Empty, whitespace, code-like and oversized IDs are rejected without silent trim; unverified dialect/version and conflicting duplicate IDs cannot emit `TranslateSay`; all four capability values must be literal `true`; the one explicit audit-fixture skip and device NOT-RUN boundary remain release blockers | e4c68f3 |
| T16 | Release test matrix and definition of done | Task 16 not executed in Task 0 | NOT-RUN: Task 16 test suite | NOT-RUN: Task 16 command | NOT-RUN | NOT-RUN | Release acceptance remains unverified until Task 16 | NOT-RUN |

Status model: `PASS` means the named requirement was verified by the listed
command and artifact; `FAIL` means the command ran and exposed a failure;
`NOT-RUN` means no verification was performed. No status may be inferred from
missing fixtures, absent tools, or an older ledger.

### Task 8 B06 step ledger

| requirementId | source requirement | Java mapping | current test/evidence | status | boundary |
|---|---|---|---|---|---|
| B06-01 | Original Task 6 Step 1: structured fixture covers dialogue, menu, name, old/new and source metadata | `RenpyTextRecord.Kind`; `text`, `kind`, `speaker`, `identifier`, `sourcePath`, `sourceLine`, `occurrence`, `coverageCertain`; `RpycTextExtractor.extractRecords` | `test_rpyc_extractor_returns_structured_records_without_breaking_text_api` plus adjacent-node speaker harness; 4-test focused command exited 0 | PASS | Python fixture and real JVM extractor compiled with repository stubs; no real APK/device claim |
| B06-02 | Original Task 6 Step 2: record model and compatibility adapter | `RenpyTextRecord` constructor normalization; `extractRecords`; `extractTexts(byte[])` and `extractTexts(byte[], boolean)` project `record.text` | Structured-record harness asserts kinds, fields, old-only filtering and legacy text API; 4-test focused command exited 0 | PASS | JVM/stub behavior only |
| B06-03 | Original Task 6 Step 3: preserve RPA virtual source path | `extractRecords(..., sourcePath, ...)` passes the caller-supplied virtual path into every record | Structured harness uses `assets/x-game/archive.rpa!/game/chapter1.rpyc` and asserts `sourcePath`; 4-test focused command exited 0 | PASS | Direct extractor path propagation; RPA enumeration and Android APK integration are not proven here |
| B06-04 | Original Task 6 Step 4: bridge returns legacy strings and structured records | `FastApkScanner.readRenpyTexts`; `RpycTextExtractor.extractRecords`; `renpyRecords` JSON plus legacy `content` | Structured test checks bridge source contract and runs extractor harness; 4-test focused command exited 0 | PASS | Source/static bridge contract plus JVM/stub extractor; not a built DEX, Android or device result |
| B06-05 | Original Task 6 Step 5: run focused and scanner regression | Existing `test_fast_scanner.py` regression gate; no production Java change justified | Focused 4-test command: PASS. Full scanner: PASS, 71 tests/0 failures. Nested discover: PASS, 179 tests/0 failures/1 explicit skip. Independent review of `237e444`: Approved. | PASS | The audit-coverage test remains skipped because its APK/extracted-text fixture is absent; no real device or real Ren'Py corpus result is inferred. |

### Task 9 B07 step ledger

| requirementId | source requirement | Java mapping | current test/evidence | status | boundary |
|---|---|---|---|---|---|
| B07-01 | Original Task 7 Step 1: cover official marked-string call forms | `RpycTextExtractor.extractRecords` recognizes `_()`, `__()`, `___()` and `_p()` source-call forms | `test_rpyc_extractor_matches_official_marked_string_forms` runs the real Java extractor harness and asserts the extracted records for all four forms; focused B07 command exited 0 | PASS | JVM/stub extractor behavior only; no claim about a real third-party game corpus |
| B07-02 | Original Task 7 Step 2: accept legal prefixes and preserve triple-quote boundaries | Java string-literal decoding and source-call argument extraction | `test_rpyc_extractor_accepts_legal_prefixes_and_keeps_triple_quote_boundaries` executes the extractor against legal prefixed strings and triple-quoted values, asserting boundaries and decoded text; focused B07 command exited 0 | PASS | Test fixture is controlled; it does not prove every Ren'Py/Python version combination |
| B07-03 | Original Task 7 Step 3: preserve exact marker keys and disambiguate equal display text | `RenpyTextRecord.text` keeps `{#slot}`/`{#menu}` in the extracted value; no key-stripping projection is used by the harness | `test_rpyc_extractor_preserves_exact_marked_string_keys_and_dynamic_uncertainty` asserts `Save{#slot}` and `Save{#menu}` remain separate exact values | PASS | Exact-key behavior is proven by the executable JVM harness; cache/compiler integration is covered by later Task 10 gates, not claimed here |
| B07-04 | Original Task 7 Step 4: keep dynamic calls uncertain | Extractor emits an uncertainty diagnostic for dynamic/non-literal marked-string calls instead of treating them as fixed text | The same exact-key/dynamic test asserts the dynamic call is reported uncertain and is not silently promoted to a fixed extracted key | PASS | Controlled dynamic-expression fixture; no real game-corpus completeness claim |
| B07-05 | Original Task 7 Step 5: run focused and regression gates | No production Java change was justified because the existing extractor passed executable evidence | Focused B07: 3 tests in 10.107s, OK. `py_compile`: exit 0. Full scanner: 71 tests in 108.792s, OK. Nested discover: 179 tests in 131.326s, OK (skipped=1). | PASS | The only skip is `audit APK/extracted texts not present`; no real APK, device, or real Ren'Py corpus result is inferred. |

### Task 10 B08 step ledger

| requirementId | source requirement | Java/UI mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| B08-01 | Original Task 8 Step 1: preserve exact old text, every occurrence, source metadata and contextual candidates | `RenpyTranslationCorpus.build(List<RenpyTextRecord>)`; `Entry.exactOld`, immutable `occurrences`, context identity from speaker/identifier/kind/source path; `contextPrompt()` now emits identifier | `test_task8_java_corpus_keeps_occurrences_and_only_marks_context_collisions`: real `javac`/`java` harness compares expected `(sourcePath, sourceLine, kind, speaker, identifier)` tuples against every repeated and collision occurrence in input order, then asserts prompt metadata | PASS | Controlled structured-record fixture and repository JVM stubs; no real game corpus claim | `54d22d1`, `4c740cf`, `bc042f0`, `33d083c`, `4a1a0a2`, `eead002` |
| B08-02 | Original Task 8 Step 2: report deterministic unique, occurrence, duplicate and collision counts | Corpus grouping plus generated `__slgTranslationCollisionReport` | `test_task8_ui_report_renders_counts_and_sanitizes_export`: unique=2, occurrences=4, duplicates=2, collisions=1; coverage fixture separately asserts 2 unique old groups and 4 occurrences | PASS | Counts are proven on controlled fixtures; audit corpus is unavailable | `54d22d1`, `4c740cf`, `bc042f0`, `33d083c`, `4a1a0a2`, `eead002` |
| B08-03 | Original Task 8 Step 3: bound collision candidates to at most three representative contexts and retain source metadata | `RenpyTranslationCorpus.contextPrompt` limits representatives to 3 and emits path/line/kind/speaker/identifier; UI collision report is a separate JS layer | The same real corpus harness creates 5 distinct contexts (`game/a.rpyc` through `game/e.rpyc`), compares exact metadata-bearing lines for each of the first three, rejects d/e, and compares two prompt outputs for exact determinism | PASS | Java prompt and UI report are tested at separate layers; no claim that every third-party corpus shape was observed | `54d22d1`, `4c740cf`, `bc042f0`, `33d083c`, `4a1a0a2`, `eead002` |
| B08-04 | Original Task 8 Step 4: reject conflicting translations before compilation and accept same exact old/same translation | `LocalTranslationSupport.translationCollisionConflict`; `TranslationCompiler.compileTranslationArtifact`; `RenpyPatchValidator` and `RpycTextExtractor` | `test_task8_compile_gate_rejects_conflicting_pairs_before_writing`: conflicting pairs fail with `translation_collision_conflict`; repeated `Save{#slot}` with the same translation is sent through the real compiler, produces non-empty RPYC, passes `RenpyPatchValidator`, and extractor finds both marker old texts | PASS | Real Java compiler/validator/extractor harness with repository stubs; no APK install/device execution | `54d22d1`, `4c740cf`, `bc042f0`, `33d083c`, `4a1a0a2`, `eead002` |
| B08-05 | Original Task 8 Step 5: render counts and export only sanitized collision metadata | `translationCollisionReport`, `JSON.stringify`, `slg-translation-collision-report`; translation values/API secrets excluded from export | UI report test verifies rendered summary contains counts and exported JSON excludes `secret`; quality 16/16, coverage 12/12 (1 explicit audit-fixture skip), workshop 59/59, scanner 71/71, nested discover 180/180 (1 explicit skip) | PASS | The only skip is `audit APK/extracted texts not present`; no real device or real third-party corpus result is inferred | `54d22d1`, `4c740cf`, `bc042f0`, `33d083c`, `4a1a0a2`, `eead002` |
| B08-06 | Original Task 8 Step 6: preserve `{#...}` marker identity in cache and compiled RPYC identity | JS `cacheScopeIdentity`, `cacheIdentity`, `cacheV2Key`, `To`, `Eo`; Java `TranslationCompiler.compileTranslationArtifact`, `RenpyPatchValidator`, `RpycTextExtractor` | Cache path PASS: generated-JS harness proves distinct keys and independent round-trips for `Save{#slot}`/`Save{#menu}`. Independent compiler/RPYC path PASS: same exact old/same translation produces non-empty validated RPYC and extractor reads both exact marker olds. Cache→compile end-to-end chaining is NOT VERIFIED | PASS | Generated-JS cache runtime plus JVM/stub compiler/RPYC fixture; no real Android/Capacitor or device claim | `54d22d1`, `4c740cf`, `bc042f0`, `33d083c`, `4a1a0a2`, `eead002` |

### Task 11 B09 step ledger

| requirementId | source requirement | Java/UI mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| B09-01 | Ren'Py tag and markup preservation | `RenpyTextValidator.validate` tag signature, balance and nesting checks; `TranslationCompiler` invokes the same validator before RPYC writing | Matrix covers valid paired markup plus missing, extra, duplicate, order-swapped and crossed tags. Invalid cases expose `tag_unbalanced` or `tag_misnested` at validator, model-acceptance and compile gates | PASS | Controlled JVM/stub strings; no claim that every third-party Ren'Py tag dialect was corpus-tested | `1cea61d` |
| B09-02 | `[expression]` and sentinel preservation | Validator interpolation-token sequence plus `LocalLlmEngine.restorePlaceholders` sentinel sequence | Matrix covers a valid `[score]` pair and missing, extra, duplicate and reordered expression tokens with `interpolation_changed`; the required scanner test also covers missing, extra, duplicate, reordered and residual sentinels through real placeholder restoration | PASS | Sentinel restoration and validator behavior are executed in JVM/stub harnesses; no model inference or device claim | `1cea61d` |
| B09-03 | Named `%(name)s` placeholder preservation | `RenpyTextValidator` printf-token sequence includes named placeholders; compiler revalidates the pair | Valid named-placeholder pair reaches compiler; missing, extra, duplicate and reordered named placeholders are rejected with `printf_changed` before acceptance/output | PASS | Controlled named-format fixtures; no claim about arbitrary Python formatting extensions outside the required syntax | `1cea61d` |
| B09-04 | `%s`, `%d`, `%1$s` printf-token preservation and ordering | `RenpyTextValidator` printf token extraction and ordered comparison | Valid pair retains all three required token forms; missing, extra, duplicate and reordered printf cases are rejected with `printf_changed` | PASS | Required token families are executablely covered; unrelated printf flags not listed in the plan are outside this gate | `1cea61d` |
| B09-05 | Model acceptance gate rejects invalid pairs before translation output | Real private `LocalLlmEngine.acceptTranslation` invoked through reflection with `TextItem`, warning list and rejection accumulator | All 18 invalid matrix cases return `false`, leave a stable family-specific code visible in warnings, and never enter the accepted translation path; all 4 valid pairs return `true` | PASS | Real Java acceptance method with repository Android/Capacitor/JSON stubs; no llama model inference was run | `1cea61d` |
| B09-06 | Compile gate rejects invalid pairs before RPYC output and accepts valid pairs | `TranslationCompiler.compileTranslationArtifact` calls validator before `compileRpyc`; returned `TranslationArtifact.rpyc` is checked | Each invalid case throws with stable code plus `old=`, `new=` and `source=` diagnostics and produces no artifact; each valid family reaches the real compiler and returns non-empty RPYC | PASS | JVM/stub compiler/RPYC path and in-memory artifact only; no APK install/device or real game corpus claim | `1cea61d` |

### Task 12 B10 step ledger

Task 12 review-remediation evidence anchor: `caf2467` adds the executable Java and generated-JS harnesses described below; `941b872` records the exact rerun counts, and this evidence keeps the explicit audit-fixture skip as `NOT-RUN`.

| requirementId | source requirement | Java/UI mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| B10-01 | Original Task 10 Step 1: count validated, rejected, uncertain, collision and file results exactly | `TranslationCoverageReport.build` consumes structured records, validator-approved translations, rejected/uncertain sets, collision candidates and classification reasons; file summaries and aggregate counters are emitted | `test_task10_java_report_counts_validated_rejected_collision_uncertain_and_files` compiles and runs the real Java report harness; exact totals are unique=6, occurrences=7, translated=1, missing=3, rejected=1, collision=1, uncertain=1; every file is checked: `a`=(1,2,1,0,0,1,0), `b`=(2,2,0,1,1,0,0), `c`=(1,1,0,0,0,0,1), `d`=(2,2,0,2,0,0,0); `topMissingFiles()` is exactly `[game/d.rpyc, game/b.rpyc]` and sanitized JSON structure/order is asserted | PASS | Repository JVM/stub execution with controlled records; no real game corpus count is inferred | `b0d0b22`, `6224e86`, `caf2467`, `941b872` |
| B10-02 | Original Task 10 Step 2: block incomplete or rejected full builds while preserving explicit incomplete-test-patch mode | Generated coverage runtime sets `blocking` from missing/rejected counts and the actual generated build branch refuses a complete build when either is non-zero; explicit incomplete mode proceeds but logs the visible incomplete-test-patch warning | `test_task10_generated_ui_build_branch_blocks_incomplete_and_warns_incomplete_patch` extracts the current generated branch and runs both cases: blocked case has compile/build calls 0/0 and `coverage incomplete`; selected incomplete case has compile/build calls 1/1 and `incomplete test patch`; static wiring test remains supplementary | PASS | Generated JS/Node behavior and Python orchestration; no installed APK build was started for this offline Task 12 | `b0d0b22`, `6224e86`, `caf2467`, `941b872` |
| B10-03 | Original Task 10 Step 3: export only sanitized coverage metadata | Coverage report JSON contains counts, file paths and diagnostics but excludes translation values, API keys and other secret material | `test_task10_generated_ui_export_sanitizes_values_and_secrets` executes current generated report construction and the real `导出覆盖率 JSON` button handler; injected `你好`/`您好`, `API-SECRET`, `PRIVATE`, and `TOKEN` are absent from exported JSON while counts/path remain | PASS | Controlled values and secret fixtures only; sanitization is not a claim about external logging outside this report path | `b0d0b22`, `6224e86`, `caf2467`, `941b872` |
| B10-04 | Original Task 10 Step 4: reset coverage state at source selection/scan and preserve exact text | `__slgResetTranslationCoverage` clears accepted/rejected/collision/uncertain/classification state; exact old text remains the key throughout report construction | `test_task10_generated_ui_reset_clears_every_coverage_state_container` executes the generated reset entry after seeding records, uncertain, classifications, validator-approved, rejected, collision candidates, collision entries and incomplete-selection state; all containers are empty/false afterward; exact-text regression remains covered by the original test | PASS | Generated JS runtime fixture; no Android lifecycle/device execution | `b0d0b22`, `6224e86`, `caf2467`, `941b872` |
| B10-05 | Original Task 10 Step 5: incremental cache/build gates do not fabricate coverage or bypass blocking | Generated `__slgTranslationCoverageIncrementalDiff` reuses unchanged validated entries; generated coverage report and the same generated build branch remain authoritative for blocking | `test_task10_incremental_cache_hit_cannot_bypass_generated_coverage_gate` executes one generated chain: `Hello` is a validated cache hit and omitted from requests; `Missing`, `New`, and `Rejected` are requested; the generated report returns missing=1/rejected=1; the generated build branch makes compile/build calls 0/0 and emits `coverage incomplete` | PASS | Repository Node/Python generated-JS fixture proves the composition; cache-to-real Android packaging is outside this offline task | `b0d0b22`, `6224e86`, `caf2467`, `941b872` |
| B10-06 | Original Task 10 Step 6: missing audit fixture remains an explicit skip/NOT-RUN boundary | `test_audit_pins_the_verified_missing_set` checks the pinned APK/dump only when both fixtures exist and otherwise calls `skipTest` | Coverage suite: `Ran 16 tests in 4.178s`, `OK (skipped=1)`; nested discover: `Ran 185 tests in 136.873s`, `OK (skipped=1)`; focused guard rejects fabricated `missingCount:0` evidence | NOT-RUN | `NOT-RUN: audit APK/extracted texts not present`; the test harness confirms the missing-fixture branch and does not fabricate a real-corpus result; release remains unapproved for this boundary | `b0d0b22`, `6224e86`, `caf2467`, `941b872` |

### Task 13 B11 step ledger

| requirementId | source requirement | Java mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| B11-01 | Original Task 11 Step 1: inspect APK-local font candidates and report required, covered and missing glyphs | `RenpyFontSupport.inspect`, safe font-entry filtering, size limits, temporary copy and best-candidate selection | `test_renpy_font_report_detects_missing_translation_glyphs` executes the Java harness against a temporary APK; two safe candidates are enumerated, the CJK candidate wins, required=3, covered=2, `界` is missing, and temporary copies are cleaned | PASS | Controlled APK ZIP and Android graphics stubs; no real APK or device font rendering claim | `798860c` |
| B11-02 | Original Task 11 Step 2: detect Chinese style resources and East Asian line-break evidence | `FontReport.hasChineseStyleBucket`, `hasEastAsianLineBreakEvidence` and warning codes | The same executable font report harness includes a Chinese style path and East Asian line-break markers and asserts both report flags plus the multiple-candidate warning | PASS | Controlled style marker fixture; it does not prove every Ren'Py style layout or device renderer | `798860c` |
| B11-03 | Original Task 11 Step 3: rewrite Chinese style font references safely and support `schinese` | `TranslationCompiler.rewriteChineseStyleFont`, `cloneChineseStyleRpyc` and RPC2 slot rebuild | `test_renpy_style_font_rewrite_rebuilds_rpc2_and_supports_schinese` rebuilds a real RPC2/pickle fixture, asserts the old font is removed, the APK-local best font is inserted, `schinese` is cloneable, and an unknown pickle is rejected | PASS | JVM/stub RPC2 fixture; no installed APK or real game launch claim | `798860c` |
| B11-04 | Original Task 11 Step 4: reset font preflight state at every new scan selection | Generated scan flow resets `__slgFontPreflightDone`, `__slgFontPreflightBlocked` and `__slgFontPreflightReport` before selection metadata is assigned | `test_font_preflight_state_resets_when_scan_selection_starts` executes the patched scan-flow extraction and checks reset ordering before the new source set is stored | PASS | Generated-JS/Python contract execution; no Android lifecycle execution | `798860c` |
| B11-05 | Original Task 11 Step 5: merge font preflight results across split APKs and keep split-owned template metadata | `FastApkScanner.scanApkSet`, `mergeFontReportsForApkSet`, `missingAcrossSet`, `bestCompatibility` and split source metadata | `test_split_scan_uses_split_template_and_merges_all_font_preflights` checks the split-aware scanner contract, merged missing set, best report and package/version/split fail-closed checks | PASS | Source-level split fixture and scanner contract; real split APK installation remains a later device gate | `798860c` |
| B11-06 | Original Task 11 Step 6: final font check must include accepted translation characters and block missing glyphs | `RenpyFontSupport.codePointsOfTranslations` is added after the baseline gate; `TranslationCompiler.compileTranslationsIntoApk` rejects an incomplete final report before writing the artifact | `test_translation_font_gate_adds_accepted_codepoints_outside_baseline` compiles and runs a protocol-2 template fixture: baseline passes, `界` is absent from baseline but present in the accepted translation, final `missingCodePoints` contains its code point, and the compiler call rejects with `renpy_font_missing_glyphs` | PASS | Controlled JVM/Android-stub fixture; no real game translation corpus, device font, or visual line-break result is inferred | `798860c` |

### Task 14 B12 step ledger

| requirementId | source requirement | Java/UI mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| B12-01 | Original Task 12 Step 1: classify compatibility as SAFE/WARNING/EXTRACT_ONLY and expose a stable unsupported snapshot | `RenpyPreflight.inspect` and `RenpyCompatibilityReport` produce the compatibility state, activation mode, issue list and blocked decision; missing source is represented as `UNSUPPORTED` + `NONE` with `renpy_preflight_missing_source` | `test_renpy_preflight_reports_safe_warning_and_extract_only` compiles/runs the Java harness for SAFE, WARNING and EXTRACT_ONLY, then verifies the unsupported snapshot, stable issue code and zero downstream calls | PASS | Controlled JVM/Android-stub report fixture; no real APK/source-set classification claim | `1d6e927` |
| B12-02 | Original Task 12 Step 2: keep report fields/order and sanitized export deterministic | `RenpyCompatibilityReport` field ordering, issue serialization and generated compatibility export | `test_renpy_compatibility_report_ui_order_fields_and_sanitized_export` executes the current report/export contract and checks field order plus removal of translation values/secrets | PASS | Generated-JS and JVM fixture export only; no external logging or installed-app export claim | `1d6e927` |
| B12-03 | Original Task 12 Step 3: EXTRACT_ONLY must block before the model call | Generated UI compatibility gate around extraction/model/build flow | `test_renpy_extract_only_compatibility_gate_blocks_before_model` checks the generated gate ordering and verifies EXTRACT_ONLY stops before model invocation | PASS | Node/generated-JS execution; no llama inference or Android lifecycle execution | `1d6e927` |
| B12-04 | Original Task 12 Step 4: font preflight must precede model use and the read bridge must return the gate | `FastApkScanner.readRenpyTexts`, generated font-preflight state and model-ordering guard | `test_renpy_font_preflight_is_before_first_model_call_and_read_bridge_returns_gate` verifies the source bridge, preflight ordering and blocked read result | PASS | Static/generated-JS contract plus controlled bridge fixture; no real font rendering or device result | `1d6e927` |
| B12-05 | Original Task 12 Step 5: unsupported input must block generated extraction, model and build calls | Generated `Ce=async()=>` gate, `E.readRenpyTexts`, model path and `E.buildPatchedApk` boundary | `test_renpy_unsupported_snapshot_blocks_generated_extract_model_and_build` extracts and executes the current generated `Ce` expression with an unsupported report; extraction/model/build counters remain zero and the gate cancels the flow | PASS | Actual generated bundle expression executed under Node with controlled bridges; no real APK build, installed device, or real corpus claim | `1d6e927` |

### Task 15 C13 step ledger

| requirementId | source requirement | Java mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| C13-01 | Original Task 13 Step 1: separate modern and protocol-2 writer dialects while preserving the modern golden | RpycPickleWriter.Dialect.PY3_MODERN delegation and explicit PY2_PROTOCOL_2 branch | test_modern_writer_is_byte_stable_with_existing_translation_pickle compiles the real writer harness and compares the modern output byte-for-byte with the pinned golden; focused C13 run is 5/5 | PASS | Controlled JVM/golden fixture; no real Ren'Py corpus claim | 31e679b |
| C13-02 | Original Task 13 Step 2: protocol-2 output uses only the Python-2-compatible opcode subset | RpycPickleWriter protocol-2 byte emission | test_protocol2_writer_uses_only_python2_compatible_opcodes uses an independent non-executing reader, rejects Python-3-only SHORT_BINUNICODE/STACK_GLOBAL, and validates the resulting RPYC structure | PASS | Byte-level reader does not deserialize or execute pickle objects; no Python 2 interpreter or device claim | 31e679b |
| C13-03 | Original Task 13 Step 3: independently verify the restricted writer capability matrix | RpycCompatibility.inspect, Report.canGenerate, GenerationSupport and TranslationCompiler dialect selection | test_protocol2_generation_support_matrix_is_verified_or_extract_only proves the verified protocol-2 shape is writable while unknown and unverified legacy shapes remain extract-only | PASS | Controlled deflated RPC2 fixtures; capability verification is intentionally narrower than a full Ren'Py interpreter | 31e679b |
| C13-04 | Original Task 13 Step 4: adversarial or malformed object graphs must fail closed | RpycCompatibility structural checks before generation | test_protocol2_adversarial_object_graph_stays_extract_only supplies recognizable names/opcodes without the required graph and asserts LEGACY_EXTRACT_ONLY; no unsafe fallback is allowed | PASS | Malformed controlled fixture; no claim that all third-party pickle variants are enumerated | 31e679b |
| C13-05 | Original Task 13 Step 5: nullable always-on language and selectable language/path must remain distinct | TranslationCompiler.compileTranslationArtifact plus RpycPickleWriter.writeNullableString | test_protocol2_always_on_artifact_uses_none_without_changing_selectable_language builds a verified protocol-2 template, checks always-on pickle NONE, tl/None paths and validator success, then checks selectable slgtranslated language and paths | PASS | Independent RPC2 slot inflate and byte inspection only; no game launch or language-menu device result | 31e679b |
| C13-06 | Original Task 13 Step 6: run regression/build gates and enforce single writer ownership | build_fast_scanner.py D8 response-file build and generated helper DEX | py_compile, scanner 74, workshop 59 and nested discover 188 (one explicit audit-fixture skip) pass; helper build exits 0 and classes7.dex contains exactly one RpycPickleWriter and one RpycCompatibility descriptor | PASS | Generated DEX and repository test fixtures; no installed APK or real-device result | 31e679b |


### Task 16 C14 step ledger

| requirementId | source requirement | Java/UI mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| C14-01 | Original Task 14 Step 1: preserve an immutable base-plus-split set and reject invalid or duplicate parts/names | InstalledApkSet constructor copies files, normalizes names and rejects missing/base-equals-split/duplicate paths and duplicate normalized split names | test_split_apk_set_is_immutable_and_rejects_duplicate_or_missing_parts plus test_split_installer_validates_metadata_abandons_write_failure_and_preserves_order execute the model; duplicate feature_a.apk/feature_a fail closed | PASS | Controlled files and JVM harness; no real installed split set claim | df89e60 |
| C14-02 | Original Task 14 Step 2: derive split names from manifest metadata and keep per-APK scan/template/font ownership | InstalledAppSource.splitNamesFromPackageInfo and FastApkScanner.scanApkSet sourceApk/apkRole/template/font merge paths | test_installed_split_selection_preserves_manifest_split_names executes two manifest-declared splits with arbitrary filenames and checks private copies; split scan/font focused contracts cover source ownership and merged reports | PASS | Controlled manifest/ZIP fixtures; no real third-party split package or device rendering claim | df89e60 |
| C14-03 | Original Task 14 Step 3: validate package/version/signature/split metadata before opening an install session | PackageInstallerSupport.validateApkSet runs before createSession | test_split_installer_validates_metadata_abandons_write_failure_and_preserves_order runs package, version, signature and split mismatch cases and asserts createSession count remains zero and the call rejects | PASS | Fake PackageManager metadata is controlled; Android system validation remains NOT-RUN | df89e60 |
| C14-04 | Original Task 14 Step 4: write base first, declared splits in order, and abandon on write failure | PackageInstallerSupport.installApkSet writes base.apk then splitEntryName values; catch path closes and abandons session | The same executable installer harness observes writes exactly [base.apk, feature_a.apk, feature_b.apk], no commit on failure and abandoned session 42 after a forced write error | PASS | Fake PackageInstaller session; no real PackageInstaller commit or user confirmation claim | df89e60 |
| C14-05 | Original Task 14 Step 5: preserve collection metadata through scan/read/compile/install UI flow | Generated workshop bridge carries baseUri, splitUris, splitNames, splitCount and sourceApk | test_split_ui_preserves_collection_metadata_through_scan_read_compile_and_install checks the current generated UI contract; split scanner/font tests keep owning source metadata | PASS | Generated-JS/source contract with controlled fixtures; no installed-app UI session claim | df89e60 |
| C14-06 | Original Task 14 Step 6: run regression/helper gates and keep real-device installation separate | build_fast_scanner.py D8 response-file build and helper DEX ownership | C14 focused 8/8, scanner 76, workshop 59, nested discover 190 (one explicit audit-fixture skip), py_compile and helper build pass; four lifecycle descriptors each occur once; real device/split install row remains NOT-RUN | PASS | No approved real split target or device install session was run; real APK install, launch, rollback and signature behavior remain release blockers | df89e60 |


### Task 17 C15 step ledger

| requirementId | source requirement | Java/UI mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| C15-01 | Original Task 15 Step 1: retain verified Say/TranslateSay identifiers and keep advanced mode default-off | `RenpyDialogueTranslation`, `RpycTextExtractor.extractDialogueTranslations`, `dialogue_id_runtime` default state and reliable-record filter | `test_dialogue_id_mode_preserves_context_specific_translations` executes the real extractor/writer harness; UI runtime starts with `__slgDialogueIdMode=false` and only records verified as `DIALOGUE`/safe ID/coverage-certain | PASS | Controlled RPYC/JVM/Node fixtures; no real game save or device launch claim | e4c68f3 |
| C15-02 | Original Task 15 Step 2: require extractor, writer, AST-version and rollback capabilities before enabling the selector | `__slgDialogueIdCapability`; strict `=== true` checks; `RpycPickleWriter.isDialogueIdWriterVerified(PY3_MODERN,17)` | `test_dialogue_id_capability_matrix_defaults_off_and_keeps_mixed_paths` iterates each missing flag and a non-boolean value; every case keeps `available=false`, `enabled=false` and mode off | PASS | Rollback flag is a controlled capability input; real save/load/rollback behavior is C15-05 and remains NOT-RUN | e4c68f3 |
| C15-03 | Original Task 15 Step 3: unsafe identifiers, unsupported dialect/version and conflicting duplicate IDs fail closed | `RenpyDialogueTranslation.requireIdentifier`; extractor safe-ID filter; `RpycPickleWriter.validateAdvancedInput` | `test_dialogue_id_safety_and_ast_matrix_fails_closed_before_translate_say` uses real `javac`/`java` harnesses for empty, leading/trailing whitespace, code-like, oversized, protocol-2, AST 16/18 and duplicate-ID cases; Node runtime test covers unsafe/collision fallback | PASS | Matrix is controlled and does not claim every third-party AST shape | e4c68f3 |
| C15-04 | Original Task 15 Step 4: keep mixed translation paths separate | `buildDialogueTranslationPickle` emits `TranslateSay` only for dialogue pairs and `TranslateString` for menu, character and marked UI pairs | The C15 Java harness asserts both context-specific dialogue IDs and all three non-dialogue string pairs remain in the mixed artifact; default global writer remains covered by the existing byte-stability test | PASS | Byte-level/JVM fixture, not a real Ren'Py interpreter or game launch | e4c68f3 |
| C15-05 | Original Task 15 Step 6: verify new game, old-save load, rollback, jump, selectable and always-on behavior | Approved real-game/device save matrix; UI must retain `__slgDialogueIdMode=false` when unavailable | Required device matrix was not run: `new game`, `translated dialogue`, `old save load`, `rollback`, `jump`, `selectable`, and `always-on` are all explicitly NOT-RUN; advanced mode is not release-approved | NOT-RUN | No approved real target/session was exercised; connected phone was deliberately not touched during offline work | e4c68f3 |
| C15-06 | Original Task 15 Steps 5 and 8: run regression, APK/helper build and single-owner DEX gates | `build_workshop_apk.py`, `build_fast_scanner.py`, generated `classes7.dex` | Focused 4/4, py_compile, scanner 77, workshop 60, nested discover 192 with one explicit audit-fixture skip, workshop APK rebuild/asset pinning, helper build and four DEX descriptor counts all pass; independent read-only reviewer returned Approved | PASS | Generated artifacts and controlled suites do not establish Android runtime, real Ren'Py corpus, Python 2 interpreter or device behavior | e4c68f3 |

Evidence boundary: the passing focused tests execute the actual Java validator, model acceptance gate, compiler/RPYC writer, dialogue-ID extractor/writer, restricted protocol-2 compatibility reader, font preflight, compatibility report and split APK lifecycle through javac/java with repository stubs and controlled ZIP/PackageInstaller fixtures, plus Python orchestration and the generated UI capability gate under Node. The built-APK suite also verifies formally rebuilt APK assets, DEX ownership, manifest, alignment and signatures. These tests do not establish a real Android/Capacitor runtime on an installed device, a real split APK installation/session/rollback, a real Ren'Py game corpus, a Python 2 interpreter result, or visual font rendering on the connected phone; the one explicit audit-fixture skip, the C15-05 save/rollback NOT-RUN row and the real-device NOT-RUN boundary remain recorded.

### Task 18 C16 machine-matrix ledger

| requirementId | source requirement | matrix/checklist mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| C16-01 | Original Task 16 Step 1: cover engine generation, support level and activation sample rows | `renpy-compatibility-matrix.md` C16-01; `renpy-release-checklist.md` C16-01 | `test_task16_matrix_and_release_checklist_cover_every_gate` checks C16 rows plus SAFE/WARNING/EXTRACT_ONLY/UNSUPPORTED and selectable/always-on tokens; C15 scanner/workshop/full discover evidence is linked | PASS | Matrix documents controlled fixture support and keeps real Python 2/3 game samples NOT-RUN | 05e32e2 |
| C16-02 | Original Task 16 Step 2: preserve archive/compiled-format rows without inventing outcomes | Matrix C16-02 covers loose RPYC2, RPA-1/RPA-2/RPA-3 and legacy zlib; unknown shapes remain blocked | Same documentation contract test; C13/C15 parser/writer evidence remains referenced; real archive/corpus fields remain `not-run` | PASS | No real third-party archive sample is claimed | 05e32e2 |
| C16-03 | Original Task 16 Step 3: keep selectable/always-on and menu strategy distinct | Matrix/checklist C16-03 records standard/custom/none and injection-failure outcomes | Same documentation contract test plus C13/T12/C15 automated evidence; device language/startup/old-save/rollback remains NOT-RUN | PASS | Controlled UI/compiler contracts do not establish real activation on a game | 05e32e2 |
| C16-04 | Original Task 16 Step 4: retain text categories, counts, coverage and font gates | Matrix/checklist C16-04 includes dialogue/menu/character/UI, `{#}`, interpolation, printf, repeated context, `missing == 0` and `rejected == 0` | Same documentation contract test; T8–T11/C15 controlled coverage/font/lint evidence is referenced | PASS | Real corpus counts and final font missing set remain NOT-RUN | 05e32e2 |
| C16-05 | Original Task 16 Step 5: record single APK and base + split build/install shape | Matrix/checklist C16-05 records `single APK`, `base + split`, build/signature/session evidence and Task 20 fresh artifact gate | Same documentation contract test; C14 lifecycle, C15 workshop/helper rebuild and asset pinning passed; T20-01..T20-03 record the fresh helper/APK/hash/signature artifact gate | PASS | Local APK/helper build is not real game install/startup/save/rollback evidence | 05e32e2; 2579bfe |
| C16-06 | Original Task 16 Step 6: derive release status from evidence and block approval on mandatory NOT-RUN | Matrix/checklist C16-06 marks final decision NOT-RUN until Tasks 19–22; checklist test rejects approval with mandatory `FAIL`/`NOT-RUN` | Two Task 18 documentation tests PASS; `py_compile test_translation_coverage.py` exit 0; `git diff --check` PASS | NOT-RUN | Final release decision still requires fresh quality/performance gates, real sample audit, device matrix and Task 23 review | 05e32e2 |

### Task 19 fresh automated release gate in the shared worktree

| requirementId | source requirement | matrix/checklist mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| T19-01 | Task 19: run the complete automated release gate as a fresh invocation in the shared worktree | `renpy-release-checklist.md` §2; `renpy-compatibility-matrix.md` §7 | `py_compile` over `patch_workshop_ui.py`, `test_workshop_patch.py`, `test_fast_scanner.py`, `test_translation_quality.py`, `test_translation_coverage.py`, `test_engine_performance.py`, `test_built_apk.py` returned 0; scanner `77` tests/0 failures; workshop `60` tests/0 failures; quality/coverage/performance `37` tests/0 failures/1 explicit `audit APK/extracted texts not present` skip; full discover `192` tests/0 failures/1 explicit skip | PASS | This is a fresh invocation in the shared checkout required by the plan; untracked test files/APK fixtures are intentionally preserved and are not claimed as part of the commit tree. The explicit skip is the absent real audit APK/extracted-text fixture. No real-game `missing == 0`, installed-device behavior, old-save loading, rollback, visual font rendering or Python 2 interpreter result is established; those remain NOT-RUN. | `66afd1c` |

### Task 20 helper DEX and workshop APK artifact gate

| requirementId | source requirement | matrix/checklist mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| T20-01 | Task 20: rebuild helper DEX and enforce unique ownership for all newly required classes | `renpy-release-checklist.md` §3; `renpy-compatibility-matrix.md` C16-05 | `python apk-work/native-fast-scan/build_fast_scanner.py` exit 0; `dexdump` on relative `generated/classes7.dex` reports exactly one Class descriptor for each of `FastApkScanner`, `RenpyTextRecord`, `RenpyTextValidator`, `TranslationCoverageReport`, `RenpyCompatibilityReport`, `RenpyPreflight`, `RenpyPatchValidator`, `RpycPickleWriter`, `InstalledApkSet`, `InstalledAppSource`, `PackageInstallerSupport`, `RenpyDialogueTranslation` | PASS | Generated helper DEX and controlled ownership checks only; no real-game APK or device behavior claim | `2579bfe` |
| T20-02 | Task 20: rebuild, sign and verify the workshop APK | `renpy-release-checklist.md` §3; `renpy-compatibility-matrix.md` C16-05 | `python build_workshop_apk.py` exit 0; `test_built_apk.py` 4/4 OK; package `com.slgtranslator.app`; `versionCode=7`; `versionName=1.0.7`; target SDK 36; APK size `34,277,494` bytes; SHA-256 `AF5D55B12AD0089C50539B99D9D370B4D197B90CCDD9CECAD83729C84C974295` | PASS | This is the local translation-tool APK, not a translated third-party Ren'Py game; no install, launch, old-save, rollback or visual-font claim | `2579bfe` |
| T20-03 | Task 20: verify APK signature after build | `renpy-release-checklist.md` §3; C16-05 signature field | `apksigner verify --verbose --print-certs` exit 0; v2=true; v3=true; one signer; certificate `Android Debug`, SHA-256 `65e8a0967857d8536fc3a79f670a7f188fe17e2568b814f8849c2fb84f069730` | PASS | Debug certificate is suitable for local artifact/install checks only; it is not a production release-key approval and does not establish device acceptance | `2579bfe` |

### Task 21/22 real-sample and device acceptance boundary

| requirementId | source requirement | matrix/checklist mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| T21-01 | Task 21: identify an approved real Ren'Py APK/corpus before static and runtime claims | `renpy-compatibility-matrix.md` §5.1; `renpy-release-checklist.md` §4/§6 | Read-only local inventory: `samples/newmanwa.apk` is `newmanwa.com`/`Manwa2`; `samples/nearbubble_final_hclm67.apk` and `selected.apk` are `com.nearbubble`/`盘丝洞`; no candidate exposed `rpyc`, `rpa`, `renpy`, `assets/game` or `libpython` entries; `com.slgtranslator.app` candidates are the translation tool | NOT-RUN | No candidate has confirmed Ren'Py provenance, permission, complete scripts and font assets; controlled fixtures and the tool APK cannot be promoted to a real-game sample | `daac060` |
| T21-02 | Task 21: execute real coverage/font/compiler/install/startup/language/old-save/rollback matrix | C16-01..C16-06 real-sample fields; release checklist §4/§7 | Not executed because T21-01 has no approved target; all real-game fields remain `NOT-RUN` | NOT-RUN | The explicit audit-fixture skip is not a real-game pass and does not establish `missing == 0` | `daac060` |
| T22-01 | Task 22: record connected-device prerequisites without conflating inventory with acceptance | `renpy-compatibility-matrix.md` §5.1; release checklist §6 | Read-only ADB inventory: `PEMM20`; Android `13`/SDK `33`; ABI `arm64-v8a,armeabi-v7a,armeabi`; `/data/user/0` available `36,952,844 KiB`; installed `com.slgtranslator.app` versionCode `10`, versionName `1.0.10` | PASS | Inventory only; no Task 20 versionCode 7 install/startup and no real game installed; no translated-text, old-save or rollback evidence | `daac060` |
| T22-02 | Task 22: perform real-device install/startup/translation/old-save/rollback and split-session workflow | C15-05/C16-05/C16-06 real-device fields | The real Ren'Py game workflow was not executed; the connected device was used only for a bounded tool-package smoke test recorded separately as T22-03 | NOT-RUN | No approved Ren'Py game APK/corpus was installed; tool-package startup and entry behavior cannot substitute for real-game translation, installation, language, save or rollback acceptance | `daac060` |
| T22-03 | Task 22: bounded device smoke for the currently installed translation tool, without installing or downgrading an APK | `renpy-release-checklist.md` §5.5; `renpy-compatibility-matrix.md` §5.1 | On `PEMM20` / Android `13` / SDK `33`, installed `com.slgtranslator.app` `versionCode=10` / `versionName=1.0.10` returned `Status: ok`, `LaunchState: COLD`, `Activity: com.slgtranslator.app/.MainActivity` from `am start -W`; the main activity stayed foreground with pid `10058`; UI hierarchy exposed the Chinese home screen, three-step workflow, APK entry and bottom navigation; tapping the entry opened the source-selection sheet; tapping “从文件选择 APK” launched `com.android.documentsui/.picker.PickActivity`; no file was selected; explicit back returned to `com.slgtranslator.app/.MainActivity`; final screenshot restored the complete home screen; app-specific recent logcat had no `FATAL EXCEPTION`, `ANR in`, `Fatal signal`, `SIGSEGV` or `Process: com.slgtranslator.app` matches. Evidence: `apk-work/device-smoke-v10-20260808-0620.png`, `apk-work/device-smoke-picker-v10-20260808-0621.png`, `apk-work/device-smoke-documentsui-v10-20260808-0623.png`, `apk-work/device-smoke-v10-final-20260808-0625.png` | PASS (scope-limited) | This proves only the installed tool v1.0.10 cold start, UI rendering, source-entry behavior, DocumentsUI invocation and return recovery. The locally rebuilt Task 20 v1.0.7 was not installed or downgraded; no APK import, scan, translation, patch install, real Ren'Py game, font, old-save, rollback, split-session or long-run behavior is claimed. Ren'Py acceptance remains NOT-RUN | `3740900` |

### Task 23 final audit and release decision

| requirementId | source requirement | matrix/checklist mapping | current test/evidence | status | boundary | commit |
|---|---|---|---|---|---|---|
| T23-01 | Task 23: re-audit every PASS/NOT-RUN row and verify evidence consistency before conclusion | `renpy-release-checklist.md` §7/§7.1; `renpy-compatibility-matrix.md` C16-06 | Focused documentation/evidence gate: 3 tests/0 failures; `py_compile` for the 7 Task 19 Python files exit 0; `git diff --check` exit 0; staged-file audit confirms only the four protected user documents remain staged | PASS | This verifies the audit records and release-state logic, not the missing external real-game/device workflow | `5d2f732` |
| T23-02 | Task 23: derive the final release decision from mandatory evidence, never from fixture-only PASS | C16-06 and final release gate table | Task 19/20 controlled gates PASS; Task 21 real APK/corpus, real coverage/font/rejected/build, Task 22 real install/startup/language/old-save/rollback, C15-05 advanced dialogue-ID device matrix, complete audit APK and production release-key approval remain `NOT-RUN` | NOT-RUN | Final decision is `未批准发布`; controlled unsupported blocking and local tool APK success are not sufficient to approve a real Ren'Py release | `5d2f732` |
