# Android UI 设计合同

## Source of truth

- 状态：Draft，待用户审阅。
- 更新时间：2026-08-10。
- 详细设计：`docs/superpowers/specs/2026-08-09-android-ui-system-upgrade-design.md`。
- 适用页面：首页、来源选择、扫描、翻译、生成结果、安装包、设置、存档和错误恢复。
- 当前事实：第一轮绿色 UI 有历史真机证据；第二轮导航、首页和版本改动尚未重新构建验收。

## Brand

- 气质：温暖、可靠、简洁，像一间整洁的玩家汉化工坊。
- 信任信号：明确状态、可恢复错误、稳定文件摘要、真实进度、局部操作反馈。
- 避免：开发者控制台、黑底绿字、夸张游戏特效、过度卡片、持续毛玻璃、点阵背景。

## Product goals

- 让普通 Android 玩家用尽可能少的决策完成 APK 选择、扫描、翻译、打包和安装。
- 让每个任务状态都说明发生了什么、下一步能做什么、失败后如何恢复。
- 在不改变业务流程、数据结构、React 控件和 Java 桥接的前提下完成视觉和交互升级。
- 以性能为硬约束：稳定节点、局部更新、有限动画、低端设备可降级。

## Non-goals

- 不迁移到 Jetpack Compose。
- 不重写 React 业务流程、Java 业务实现、桥接接口或持久化结构。
- 不把 Remotion 放入 APK 运行时。
- 没有 DSN、隐私策略和网络策略时不强行接入 Sentry。
- 不把未采样的 60FPS、横屏、平板或 clean-room 构建写成已完成。

## Personas and jobs

- 主要用户：不熟悉 APK 解包、Ren'Py 文本结构和 AI 参数的普通 Android 玩家。
- 核心任务：选择 APK，知道扫描状态，配置翻译服务，等待翻译和打包，安装或保存结果，在失败后重试或恢复。
- 使用场景：单手手机操作、长任务等待、网络不稳定、存储空间有限、深色模式、系统字体放大和 Back 手势。

## Information architecture

- 顶层导航：首页、安装包、我的。
- 首页：空状态、来源选择、未完成任务恢复。
- 任务状态：`idle`、`scanning`、`empty`、`ready`、`translating`、`patching`、`completed`、`failed`。
- 我的：翻译服务、存档转移、导入存档、清理安装包与旧缓存、关于。
- 规则：任务进行中隐藏顶层导航；弹层打开时 Back 先关闭弹层；返回任务保留现有恢复信息。

## Design principles

1. 一屏一个核心决策。
2. 状态优先于装饰，先说清楚发生了什么。
3. 局部反馈优先于整页重绘。
4. 动画解释状态和连续性，不制造等待。
5. 低端设备上的响应速度优先于视觉特效。
6. 通过稳定语义锚点实现共享元素连续性，不移动或克隆 React 节点。

## Visual language

- 颜色：使用 Material 3 语义角色；主色为克制橄榄绿；浅色和深色分别定义完整 token。
- 浅色回退：背景 `#F7F8F2`、表面 `#FFFFFF`、正文 `#263027`、辅助文字 `#667268`、主色 `#55724D`、错误 `#B3261E`。
- 深色回退：背景 `#20251F`、表面 `#2A302A`、正文 `#EFF2EA`、辅助文字 `#B8C1B6`、主色 `#A2C89B`、错误 `#FFB4AB`。
- 字体：系统无衬线、Roboto 或 Noto Sans SC；正文 15 至 16px；标题 20px；首页主标题 26 至 28px；字距为 0。
- 间距：4/8/12/16/24/32px；页面边距紧凑宽度 16px，展开宽度 24px。
- 形状：卡片 16px，输入和列表 12px，弹层顶部 24px；触控目标至少 48px。
- 层级：最多两层轻阴影；不使用卡片套卡片；不默认使用 `backdrop-filter`。
- 动效：按钮 100 至 120ms，状态 160 至 200ms，列表 180 至 240ms，弹层 220 至 260ms，普通交互不超过 400ms。

## Components

- 稳定任务壳层：顶部栏、文件摘要、状态区、进度区、详情区、操作区、播报区。
- 通用状态：加载、空、错误、成功、禁用、忙碌、减少动画。
- 通用页面：统一返回栏、空状态、错误状态、列表行、底部导航和底部弹层。
- 图标：24px 线性语义图标；图标按钮必须有 `aria-label`，不能只靠图标或颜色表达状态。
- 组件所有权：可见层和 token 归 `patch_workshop_ui.py`；业务副作用归 React/Java；契约归 `test_workshop_patch.py`。

## Accessibility

- 目标：WCAG AA 对比度和可操作性。
- 所有操作目标至少 48px，相邻目标至少 8px。
- 使用 `aria-label`、`aria-live`、`aria-atomic`、`aria-busy` 和 `aria-invalid` 表达状态。
- 支持深色模式、系统字体放大、系统减少动画和 Android Back。
- 状态必须同时使用文字、图标和结构表达，不能只使用颜色。
- 长文件名可省略，但完整值必须能在详情中查看。

## Responsive behavior

- 紧凑宽度 `<600px`：单列内容、16px 边距、贴边底部导航。
- 中等宽度 `600px-719px`：内容最大 680px，步骤区三列，弹层居中。
- 展开宽度 `>=720px`：内容最大 760px，导航栏最大 560px 居中，不使用毛玻璃。
- 横屏矮屏：压缩品牌区和顶部间距，主操作必须保持可见。
- 大字体：允许换行和增高，不用固定高度裁剪文字或按钮。

## Interaction states

- Loading：只覆盖必要区域；不用无限 shimmer；刷新失败保留旧内容。
- Empty：说明为什么为空，并提供一个明确主操作。
- Error：说明原因、下一步和可执行动作；原始诊断放在详情。
- Success：显示结果摘要，主操作为立即安装，次操作为保存 APK。
- Disabled：只禁用当前不可用操作，不让整页失去响应。
- Slow/offline：保留恢复信息，归类为可理解的网络或服务错误。

## Implementation constraints

- 技术边界：现有 React WebView 注入层、CSS/JS runtime、Java 原生桥接。
- 业务不变量：选择、扫描、预检、翻译、打包、安装/保存顺序不变；现有字段和插件方法不变。
- 性能：单 `MutationObserver`，使用 `requestAnimationFrame` 合并刷新；进度只更新局部节点；关键交互按 16.7ms 帧预算观察。
- 当前风险：历史 settings 样本为 17 帧、10 帧卡顿、50 分位 32ms，必须重新测量，不能直接视为通过。
- 兼容性：CSS 高级颜色函数必须有回退；不提高现有 minSdk/targetSdk；保留系统 inset、文件选择器和安装页。
- 验收：契约测试、Node 语法、构建/签名验证、真机截图、UI dump 和 `gfxinfo`；未测项明确标为未验证。
- 可观测性：Sentry 只有在 DSN、隐私和网络策略齐备后可选接入，且不得记录 API Key、文件内容或翻译文本。

## Open questions

- [ ] 用户是否确认方案 B 的首页结构，以及去除大渐变、点阵和浮动毛玻璃的边界。
- [ ] 是否能提供 canonical base APK，以便后续进行 clean-room build。
- [ ] 是否有可用于横屏、展开宽度、大字体和减少动画验收的设备。

## Release gate

本文是设计合同，不是实现完成声明。用户审阅确认后，才能根据详细 spec 生成最终实施计划；实施计划完成后才能修改代码和构建 APK。
